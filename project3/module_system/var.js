const odd = '홀수';
const even = '짝수';

// 모듈 시스템
module.exports = {
  odd,
  even,
};

// module.exports = 값(내보내는 것)
// → 모듈에 해당하는, 내보낼 값을 할당한다는 의미
// → module이라는 객체 안에e exports 속성이 있고 해당 속성에 객체를 할당하는 것

// exports.even = even
// exports.odd = odd
// → 위와 같은 코드
