const process = require('process'); // 현재 동작중이 node process에 대한 정보를 담고 있는 객체

setTimeout(() => {
  console.log('setTimeout');
}, 0);

process.nextTick(() => {
  console.log('nextTick');
});

for (let i = 0; i < 1000; i++) {
  console.log('for loop');
}
