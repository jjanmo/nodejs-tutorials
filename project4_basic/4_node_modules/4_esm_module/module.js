// import { addCount, getCount } from './counter.js';
import * as counter from './counter.js';
console.dir(counter);

counter.addCount();
counter.addCount();
counter.addCount();

console.log(counter.getCount()); // 3
