let count = 0;

const addCount = () => {
  count++;
};

const getCount = () => count;

module.exports.addCount = addCount;
// exports.addCount = addCount; : exports 객체에 직접 할당해도 동일한 결과,  생략 가능
// exports = {} : exports 객체를 새로운 객체로 덮어쓰면 module.exports와 exports는 더 이상 같은 객체를 참조하지 않음
module.exports.getCount = getCount;

/**
 * module 객체 안에 많은 정보가 들어있음
 * module.exports는 module 객체의 속성으로 exports 객체를 참조하고 있음 → module 외부로 내보낼 객체를 정의하는 것
 * 해당 파일을 require하는 파일에서 module.exports로 정의한 객체를 사용할 수 있음
 */
console.log(module);
